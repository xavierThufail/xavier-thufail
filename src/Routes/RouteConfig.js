import {
  DashboardContainer
} from '../Containers';
import { routePaths } from '../Constants';

const routeConfigs = [
  {
    path: routePaths.DASHBOARD,
    component: DashboardContainer
  }
];

export default {
  routeConfigs
};
